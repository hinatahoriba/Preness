<p><%= @resource.email %> 様</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>メールアドレス変更の申請を受け付けました。変更先: <%= @resource.unconfirmed_email %></p>
<% else %>
  <p>メールアドレスが変更されました。現在のメールアドレス: <%= @resource.email %></p>
<% end %>
