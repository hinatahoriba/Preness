<%= @resource.email %> 様

パスワード再設定のリクエストを受け付けました。

以下のリンクから新しいパスワードを設定してください。
<%= edit_password_url(@resource, reset_password_token: @token) %>

このメールに心当たりがない場合は、そのまま破棄してください。
上記リンクにアクセスしない限り、パスワードは変更されません。
