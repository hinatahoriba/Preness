<% content_for :body_class, "bg-[#f9fafb] text-[#374151] h-screen" %>

<div class="flex h-full">
  <%# サイドバー %>
  <aside class="w-60 bg-white border-r border-gray-100 flex flex-col h-full">
    <div class="p-6">
      <h1 class="text-xl font-black italic text-gray-800 tracking-tighter text-center">Preness</h1>
    </div>

    <nav class="flex-1 px-3 space-y-1">
      <%= link_to mypage_path, class: "flex items-center space-x-3 text-gray-400 hover:bg-gray-50 px-4 py-2.5 rounded-xl font-medium transition" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
        <span class="text-sm">ダッシュボード</span>
      <% end %>

      <%= link_to "#", class: "flex items-center space-x-3 text-gray-400 hover:bg-gray-50 px-4 py-2.5 rounded-xl font-medium transition" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
        <span class="text-sm">学習履歴</span>
      <% end %>

      <%= link_to account_setting_path, class: "flex items-center space-x-3 bg-blue-50 text-blue-600 px-4 py-2.5 rounded-xl font-bold" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
        <span class="text-sm">アカウント設定</span>
      <% end %>

      <%= link_to "#", class: "flex items-center space-x-3 text-gray-400 hover:bg-gray-50 px-4 py-2.5 rounded-xl font-medium transition" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
        <span class="text-sm">サポート・規約</span>
      <% end %>
    </nav>

    <div class="p-6 border-t border-gray-50">
      <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "flex items-center space-x-3 text-gray-400 hover:text-red-500 transition text-sm font-medium w-full px-4" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path></svg>
        <span>ログアウト</span>
      <% end %>
    </div>
  </aside>

  <%# メインコンテンツ %>
  <main class="flex-1 p-8 overflow-y-auto">
    <div class="max-w-lg mx-auto">
      <div class="mb-6">
        <%= link_to account_setting_path, class: "inline-flex items-center space-x-1 text-sm text-gray-500 hover:text-gray-700 font-medium mb-4 transition" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
          <span>アカウント設定に戻る</span>
        <% end %>
        <h2 class="text-2xl font-bold mb-2">パスワード変更</h2>
        <p class="text-sm text-gray-500">新しいパスワードを設定します。</p>
      </div>

      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-5" }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <%= f.hidden_field :email, value: current_user.email %>

          <div>
            <div class="flex items-baseline justify-between">
              <%= f.label :password, "新しいパスワード", class: "text-sm font-medium text-slate-700" %>
              <% if @minimum_password_length %>
                <span class="text-xs text-slate-400">最低<%= @minimum_password_length %>文字</span>
              <% end %>
            </div>
            <%= f.password_field :password, autocomplete: "new-password", class: "mt-1 w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-slate-900 placeholder-slate-400 outline-none ring-0 transition focus:border-preness-400 focus:ring-2 focus:ring-preness-300/40" %>
          </div>

          <div>
            <%= f.label :password_confirmation, "新しいパスワード（確認）", class: "text-sm font-medium text-slate-700" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "mt-1 w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-slate-900 placeholder-slate-400 outline-none ring-0 transition focus:border-preness-400 focus:ring-2 focus:ring-preness-300/40" %>
          </div>

          <div>
            <%= f.label :current_password, "現在のパスワード（確認のため必須）", class: "text-sm font-medium text-slate-700" %>
            <%= f.password_field :current_password, autocomplete: "current-password", class: "mt-1 w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-slate-900 placeholder-slate-400 outline-none ring-0 transition focus:border-preness-400 focus:ring-2 focus:ring-preness-300/40" %>
          </div>

          <%= f.submit "保存する", class: "inline-flex w-full items-center justify-center rounded-xl bg-preness-500 px-4 py-3 text-sm font-semibold text-white transition hover:bg-preness-600" %>
        <% end %>
      </div>
    </div>
  </main>
</div>
